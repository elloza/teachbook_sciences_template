# 2. Flujo de Trabajo

Este es el ciclo de vida recomendado para tu TeachBook:

1. **Escribir contenido** en archivos Markdown (`.md`) o Notebooks (`.ipynb`).
2. **Previsualizar** los cambios localmente (opcional, pero recomendado).
3. **Guardar versiones** (commit) con Git.
4. **Publicar** (push) a GitHub para que la web se actualice automáticamente.

## Estructura de archivos
El contenido se organiza en la carpeta `book/es/`.
- `intro.md`: Página principal.
- `_toc.yml`: Índice del libro.
- `_config.yml`: Configuración general.
